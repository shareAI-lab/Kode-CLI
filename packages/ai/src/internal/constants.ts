/**
 * LLM constants owned by @kode/ai so conversion/query modules can avoid
 * pulling the full core LLM stack for trivial string/number defaults.
 */

export const API_ERROR_MESSAGE_PREFIX = 'API Error'
export const MAIN_QUERY_TEMPERATURE = 1
